* Establish what the DB should look like
* Figure out a good interface for the daemon
	- research that command line arg parser
* Find good authentication system
* Figure out how to parse the input properly (strange delimiter + escaping)
* Create tests for the remainder of the sisyl_client library
* Reformat makefile to be "proper"
* create struct for result from process_args function
* isolate query_daemon function from process_args function

